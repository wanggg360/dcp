import { defineConfig } from 'umi';

export default defineConfig({
  define: {

    USER_DETAILS_TAG: 'userdetails',  // localstorage中的用户信息标签

    BACKEND_SERVER_URL: 'http://127.0.0.1:8082',
  },

});
